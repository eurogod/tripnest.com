fastapi==0.111.0
uvicorn[standard]==0.30.1
deepface==0.0.93
tensorflow==2.16.1
# TensorFlow 2.16 ships Keras 3; DeepFace/RetinaFace still require the Keras-2 API.
# Without this the service crashes on startup ("requires tf-keras package").
tf-keras==2.16.0
pydantic==2.7.4
requests==2.32.3
python-multipart==0.0.9
# DeepFace's anti-spoofing/liveness model (MiniFASNet, used via extract_faces(anti_spoofing=True))
# is implemented in PyTorch. Install the CPU-only wheel to avoid pulling ~2GB of CUDA libs:
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
